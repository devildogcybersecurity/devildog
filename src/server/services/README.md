# Services

Place server-side business services here.

The template includes `projectsService.ts` as a reference point for validation, slug generation,
and repository orchestration that should stay out of page components.
