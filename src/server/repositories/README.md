# Repositories

Place database access repositories here. Database access must remain server-only.

The template includes `projectsRepository.ts` as the starter pattern for Prisma-backed reads and
writes. Keep repositories focused on persistence concerns and move orchestration into services.
